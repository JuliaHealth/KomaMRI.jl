import{_ as i,o as a,c as s,aA as t}from"./chunks/framework.DSs4OLzh.js";const u=JSON.parse('{"title":"GPU Parallelization","description":"","frontmatter":{},"headers":[],"relativePath":"explanation/7-gpu-explanation.md","filePath":"explanation/7-gpu-explanation.md","lastUpdated":null}'),n={name:"explanation/7-gpu-explanation.md"};function o(l,e,h,r,p,d){return a(),s("div",null,[...e[0]||(e[0]=[t(`<h1 id="GPU-Parallelization" tabindex="-1">GPU Parallelization <a class="header-anchor" href="#GPU-Parallelization" aria-label="Permalink to &quot;GPU Parallelization {#GPU-Parallelization}&quot;">​</a></h1><p>KomaMRI uses a vendor agnostic approach to GPU parallelization in order to support multiple GPU backends. Currently, the following backends are supported:</p><ul><li><p>CUDA.jl (Nvidia)</p></li><li><p>Metal.jl (Apple)</p></li><li><p>AMDGPU.jl (AMD)</p></li><li><p>oneAPI.jl (Intel)</p></li></ul><h2 id="Choosing-a-GPU-Backend" tabindex="-1">Choosing a GPU Backend <a class="header-anchor" href="#Choosing-a-GPU-Backend" aria-label="Permalink to &quot;Choosing a GPU Backend {#Choosing-a-GPU-Backend}&quot;">​</a></h2><p>To determine which backend to use, KomaMRI uses <a href="https://pkgdocs.julialang.org/v1/creating-packages/#Conditional-loading-of-code-in-packages-(Extensions)" target="_blank" rel="noreferrer">package extensions</a> (introduced in Julia 1.9) to avoid having the packages for each GPU backend as explicit dependencies. This means that the user is responsible for loading the backend package (e.g. <code>using CUDA</code>) at the beginning of their code, or prior to calling KomaUI(), otherwise, Koma will default back to the CPU:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> KomaMRI</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> CUDA </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># loading CUDA will load KomaMRICoreCUDAExt, selecting the backend</span></span></code></pre></div><p>Once this is done, no further action is needed! The simulation objects will automatically be moved to the GPU and back once the simulation is finished. When the simulation is run a message will be shown with either the GPU device being used or the number of CPU threads if running on the CPU.</p><p>Of course, it is still possible to move objects to the GPU manually, and control precision using the f32 and f64 functions:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">x </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> rand</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">100</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">x </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> f32 </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> gpu </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Float32 CuArray</span></span></code></pre></div><p>To change the precision level used for the entire simulation, the <code>sim_params[&quot;precision&quot;]</code> parameter can be set to either <code>f32</code> or <code>f64</code> (Note that for most GPUs, Float32 operations are considerably faster compared with Float64). In addition, the <code>sim_params[&quot;gpu&quot;]</code> option can be set to true or false to enable / disable the gpu functionality (if set to true, the backend package will still need to be loaded beforehand):</p><p>Two other simulation parameters, <code>gpu_groupsize_precession</code> and <code>gpu_groupsize_excitation</code> are exposed to allow adjusting the number of threads in each threadgroup within the <code>run_spin_precession!</code> and <code>run_spin_excitation!</code> gpu kernels. By default, they are both 256, however, on some devices other values may result in faster performance. The gpu groupsize must be a multiple of 32 between 32 and no higher than 1024, otherwise the simulation will throw an error.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> KomaMRI</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> CUDA</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sys </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Scanner</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">obj </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> brain_phantom2D</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">seq </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> PulseDesigner</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">EPI_example</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#Simulate on the GPU using 32-bit floating point values</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sim_params </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Dict{String,Any}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;Nblocks&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 20</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;gpu&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;precision&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;f32&quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;sim_method&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Bloch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">simulate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(obj, seq, sys; sim_params)</span></span></code></pre></div><h2 id="How-Objects-are-moved-to-the-GPU" tabindex="-1">How Objects are moved to the GPU <a class="header-anchor" href="#How-Objects-are-moved-to-the-GPU" aria-label="Permalink to &quot;How Objects are moved to the GPU {#How-Objects-are-moved-to-the-GPU}&quot;">​</a></h2><p>Koma&#39;s <code>gpu</code> function implementation calls a separate <code>gpu</code> function with a backend parameter of type <code>&lt;:KernelAbstractions.GPU</code> for the backend it is using. This function then calls the <code>fmap</code> function from package <code>Functors.jl</code> to recursively call <code>adapt</code> from package <code>Adapt.jl</code> on each field of the object being transferred. This is similar to how many other Julia packages, such as <code>Flux.jl</code>, transfer data to the GPU. However, an important difference is that KomaMRI adapts directly to the <code>KernelAbstractions.Backend</code> type in order to use the <code>adapt_storage</code> functions defined in each backend package, rather than defining custom adapters, resulting in an implementation with fewer lines of code.</p><h2 id="Inside-the-Simulation" tabindex="-1">Inside the Simulation <a class="header-anchor" href="#Inside-the-Simulation" aria-label="Permalink to &quot;Inside the Simulation {#Inside-the-Simulation}&quot;">​</a></h2><p>KomaMRI has three different simulation methods, all of which can run on the GPU:</p><ul><li><p><code>BlochSimple</code>: <a href="https://github.com/JuliaHealth/KomaMRI.jl/blob/master/KomaMRICore/src/simulation/SimMethods/BlochSimple/BlochSimple.jl" target="_blank" rel="noreferrer">BlochSimple.jl</a></p></li><li><p><code>BlochDict</code>: <a href="https://github.com/JuliaHealth/KomaMRI.jl/blob/master/KomaMRICore/src/simulation/SimMethods/BlochDict/BlochDict.jl" target="_blank" rel="noreferrer">BlochDict.jl</a></p></li><li><p><code>Bloch</code>: <a href="https://github.com/JuliaHealth/KomaMRI.jl/blob/master/KomaMRICore/src/simulation/SimMethods/Bloch/BlochCPU.jl" target="_blank" rel="noreferrer">BlochCPU.jl</a> / <a href="https://github.com/JuliaHealth/KomaMRI.jl/blob/master/KomaMRICore/src/simulation/SimMethods/Bloch/BlochGPU.jl" target="_blank" rel="noreferrer">BlochGPU.jl</a></p></li></ul><p><code>BlochSimple</code> uses array-based methods which are simpler to understand compared with the more optimized <code>Bloch</code> implementation.</p><p><code>BlochDict</code> can be understood as an extension to <code>BlochSimple</code> that outputs a more detailed signal.</p><p><code>Bloch</code> is equivalent to <code>BlochSimple</code> in the operations it performs, but has separate implementations optimized for both the CPU and GPU. The CPU implementation uses array broadcasting for computation and preallocates all simulation arrays to conserve memory. The simulation arrays are 1-dimensional with length equal to the number of spins in the phantom and are updated at each time step. The GPU implementation also uses preallocation and a similar loop-based computation strategy, but does so using kernels for spin precession and excitation implemented using the <code>KernelAbstractions.jl</code> package. A key advantage of using kernel-based methods is that intermediate values compuated based on phantom and sequence properties can be stored in registers without having to write back to GPU global memory, which has much higher memory latency compared with the CPU. Other optimizations within the kernels include:</p><ul><li><p>Reducing the output signal value at each time step within the kernel so that the first thread for each thread group writes the sum of the signal values for each thread in the threadgroup to GPU global memory. This reduces the number of GPU global memory reads + writes needed for the output signal from <code>Number of Spins x Number of Time Points</code> to <code>Number of Spins x Number of Time Points / Number of Threads in Threadgroup</code>, improving scalability for large phantom objects.</p></li><li><p>Using julia&#39;s <code>Val</code> type to specialize at compile-time on properties unique to the simulation inputs. For example, whether the phantom exibits spin motion is passed as either <code>Val(true)</code> or <code>Val(false)</code> to the precession kernel so that different kernels will be compiled for phantoms with or without motion. For the kernel compiled for phantoms without motion, there will be no runtime check of the motion type of the phantom, and everything inside the <code>if MOTION</code> statements in the kernel will be compiled out, saving register space and enabling further compiler optimizations. This strategy enables adding support in the future for less common use cases without negatively impacting performance for simulations not using these features.</p></li><li><p>Since GPU registers are limited and can hurt GPU occupancy if a kernel uses a high number, their use is minimized by working with real and imaginary components directly rather than abstracting complex number math, using unsigned int32 literal values instead of Julia&#39;s default <code>Int64</code>, and inlining all functions called from within the kernels.</p></li></ul><p>The performance differences between Bloch and BlochSimple can be seen on the KomaMRI <a href="https://juliahealth.org/KomaMRI.jl/benchmarks/" target="_blank" rel="noreferrer">benchmarks page</a>. The first data point is from when <code>Bloch</code> was what is now <code>BlochSimple</code>, before a more optimized implementation was created. The following pull requests are primarily responsible for the performance differences between <code>Bloch</code> and <code>BlochSimple</code>:</p><ul><li><p><a href="https://github.com/JuliaHealth/KomaMRI.jl/pull/443" target="_blank" rel="noreferrer">(443) Optimize run_spin_precession! and run_spin_excitation! for CPU</a></p></li><li><p><a href="https://github.com/JuliaHealth/KomaMRI.jl/pull/459" target="_blank" rel="noreferrer">(459) Optimize run_spin_precession! for GPU</a></p></li><li><p><a href="https://github.com/JuliaHealth/KomaMRI.jl/pull/462" target="_blank" rel="noreferrer">(462) Optimize run_spin_excitation! for GPU</a></p></li><li><p><a href="https://github.com/JuliaHealth/KomaMRI.jl/pull/537" target="_blank" rel="noreferrer">(537) Faster Bloch GPU</a></p></li></ul>`,23)])])}const k=i(n,[["render",o]]);export{u as __pageData,k as default};
